# Zusätzliche Abhängigkeiten NUR für die RAGAS-Evaluation
# (scripts/evaluate_ragas.py). Nicht für den normalen Betrieb der App nötig.
#
# Installation: pip install -r requirements-eval.txt
#
# Hinweis: ragas 0.4.x importiert unbedingt langchain_community.chat_models
# .vertexai, das in aktuellen langchain-community-Versionen (Sunsetting)
# entfernt wurde. Ein Kompatibilitäts-Stub dafür liegt in
# src/rag_manual_bot/eval/_ragas_compat.py und wird von allen Eval-Skripten
# automatisch vor dem ragas-Import geladen.

ragas>=0.4.3
pandas>=2.0.0
tabulate>=0.9.0
