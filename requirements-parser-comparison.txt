# Zusätzliche, schwere Abhängigkeiten NUR für den einmaligen Aufbau des
# Parser-Vergleichs-Demo-Korpus (scripts/ingest_parser_comparison.py).
# Nicht für den normalen Betrieb der App nötig (siehe requirements.txt).
#
# Installation: pip install -r requirements-parser-comparison.txt

docling>=2.100.0
pdfplumber>=0.11.0
unstructured[pdf]>=0.25.0
