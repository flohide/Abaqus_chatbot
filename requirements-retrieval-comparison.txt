# Zusätzliche Abhängigkeiten NUR für die "rerank"- und "hybrid"-
# Retrieval-Strategien im Retrieval-Vergleich (scripts/evaluate_retrieval_ragas.py,
# app.py Sidebar "Retrieval-Strategie"). "mmr"/"similarity" brauchen nichts
# zusätzlich. Nicht für den normalen Betrieb der App nötig (siehe
# requirements.txt).
#
# Installation: pip install -r requirements-retrieval-comparison.txt
#
# rank_bm25: reine Python-Implementierung von BM25 (Keyword-Suche) für die
# "hybrid"-Strategie, keine Downloads/Modelle nötig.
# sentence-transformers: für den Cross-Encoder in der "rerank"-Strategie
# (lädt beim ersten Aufruf ein Modell vom HuggingFace Hub, siehe
# docs/RETRIEVAL.md) - bereits vorhanden, falls
# requirements-embedding-comparison.txt installiert ist.

rank_bm25>=0.2.2
sentence-transformers>=3.0.0
